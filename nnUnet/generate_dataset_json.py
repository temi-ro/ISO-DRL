# File entirely generated by Google Gemini 3 (For more info, see report Section 7.1)

# This script generates a dataset.json file for the nnU-Net framework based on the structure of the BraTS2023 dataset.

from nnunetv2.dataset_conversion.generate_dataset_json import generate_dataset_json
import os

# Configure paths and dataset name
nnUNet_raw = "."
dataset_name = "Dataset002_BraTS2023" # Match the name above
imagesTr_dir = os.path.join(nnUNet_raw, dataset_name, "imagesTr")

# Generate json
generate_dataset_json(
    os.path.join(nnUNet_raw, dataset_name), 
    {0: "MRI_T1c"},  # Channel names
    {"background": 0, "Tumor": 1}, # Labels
    len(os.listdir(imagesTr_dir)), 
    '.nii.gz', 
    dataset_name=dataset_name
)